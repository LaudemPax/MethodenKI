\section{Python-Projekte}

Neben dem Java-Projekt wurde auch Python-Code verwendet. Neben dem Java-Projekt wurde auch Python-Code verwendet. Die Dateien sind unter vertiefungen-projekte/python zu finden. Jedes Projekt wird in den jeweiligen Kapiteln ausführlich erläutert (Kapitel \ref{section:classifier-project} und \ref{section:q-learning-vertiefung}) und kann wie folgt ausgeführt werden:

\begin{lstlisting}
# first install the required libraries
pip install -r requirements.txt

# then run
python3 <filename>.py
\end{lstlisting}